<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            <strong>{{user.username}}</strong> on {{format_date created_at}}
        </div>
        <div class="text">
            {{comment_body}}
        </div>
    </section>
    {{/each}}
</div>